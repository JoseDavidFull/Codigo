/*Escribir una función que tome una frase 
como parámetro y devuelva la misma 
frase pero en orden inverso.
function reverse(){
    const array = ["Palabra",25]
    array.reverse();   
    return array;
}
console.log(reverse());*/

/*Escribir una función que tome un número 
como parámetro y devuelva true si es par,
o false si es impar.

function ParImpar(a) {
    var valor;
    if(a%2==0){
        valor = "Par";
    }
    else{
        valor = "Impar";
    }
    return valor;
}
console.log(ParImpar(4));*/

/*Escribir una función que tome un número 
como parámetro y devuelva la factorial 
de ese número.
function factorial() {
    var fac =1;
    var n=5;
    for(let i=1;i<=n;i++){
        fac*=i;
    } 
    return fac
}
console.log(factorial());*/

